apiVersion: v1
kind: Service
metadata:
  name: mongo-service
spec:
  type: NodePort
  selector:
    run: server
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      nodePort: 30002
